const express = require('express'); const app = express(); const ordersDB = { '101': { orderId: '101', userId: 'user_a', item: 'Laptop', price: '$1000' }, '102': { orderId: '102', userId: 'user_b', item: 'Mobile Phone', price: '$500' } }; app.get('/api/orders/:orderId', (req, res) => { const order = ordersDB[req.params.orderId]; if (order) { res.json({ success: true, data: order }); } else { res.status(404).json({ success: false, message: 'Order not found' }); } }); app.listen(3000, () => console.log('Vulnerable Server running on http://localhost:3000'));
